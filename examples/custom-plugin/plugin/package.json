{
  "name": "tauri-specta-custom-plugin",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "postinstall": "pnpm build",
    "build": "pnpm tsc",
    "codegen": "cargo test && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
